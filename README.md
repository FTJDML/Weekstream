# Optional: better summaries with OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Display timezone
DIGEST_TIMEZONE=Europe/Amsterdam

# Optional e-mail delivery
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_STARTTLS=true
SMTP_SSL=false
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=
EMAIL_TO=you@example.com
EMAIL_SUBJECT_PREFIX=AI News Digest

# Optional logging
LOG_LEVEL=INFO
